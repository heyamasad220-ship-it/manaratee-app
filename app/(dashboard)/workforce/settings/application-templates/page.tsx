import { redirect } from "next/navigation"

/** Application Templates hub removed — each type lives under its category Applications tab. */
export default function WorkforceSettingsApplicationTemplatesRedirectPage() {
  redirect("/workforce")
}
