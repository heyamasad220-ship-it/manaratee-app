import { VendorHubSectionPlaceholder } from "@/components/vendor-hub/vendor-hub-section-placeholder"

export default function BoothMapPage() {
  return (
    <VendorHubSectionPlaceholder
      title="Booth Map"
      description="Visual map of booth locations and assignment status."
      todo="Add map coordinates to vendor_hub_booths or a layout config table, then render an interactive map with list/map toggle."
    />
  )
}
